import { StockLevelPipe } from './stock-level.pipe';

describe('StockLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new StockLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
